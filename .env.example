# DoorDash Analytics Case — Environment Variables
# Copy to .env and adjust as needed: cp .env.example .env

# dbt profiles directory (relative to project root)
DBT_PROFILES_DIR=./dbt_doordash

# Streamlit server port
STREAMLIT_PORT=8501
